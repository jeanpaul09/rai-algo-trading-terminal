# RAI-ALGO Framework Dependencies
# Core dependencies for live trading bot

# HTTP requests for exchange APIs
requests>=2.31.0

# FastAPI and server
fastapi>=0.104.0
uvicorn[standard]>=0.24.0
pydantic>=2.0.0

# Note: Standard library modules used:
# - hmac (for API signatures)
# - hashlib (for hashing)
# - threading (for concurrent execution)
# - logging (for logging)
# - dataclasses (for data structures)
# - abc (for abstract base classes)
# - enum (for enums)
# - datetime (for timestamps)
# - typing (for type hints)
